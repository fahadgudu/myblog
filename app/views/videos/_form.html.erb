<%= form_for(@video, html: {multipart: true}) do |p| %>

    <% if @video.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@video.errors.count, "error") %> prohibited
            this video from being saved:
          </h2>
          <ul>
            <% @video.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <p>
      <%= p.fields_for :video_attach do |f| %>
          <%= f.label :video_attach %>
          <%= f.file_field :data %>
      <% end %>
    </p>
    <p>
      <%= p.label :title %><br>
      <%= p.text_field :title %>
    </p>
    <p>
      <%= p.submit %>
    </p>

<% end %>